# herdr 中文汉化说明（i18n）

本项目在 herdr 原版基础上引入了 [rust-i18n](https://github.com/longbridge/rust-i18n) 框架，实现了 UI 界面的中文汉化，并保留随时切回英文的能力。本文档说明汉化的工作原理、如何编译、以及如何在上游更新时维护中文翻译。

## 一、用户使用

在配置文件的 `[ui]` 段设置语言。Linux/macOS 默认路径是
`~/.config/herdr/config.toml`，Windows 默认路径是
`%APPDATA%\herdr\config.toml`：

```toml
[ui]
language = "zh"    # 简体中文（默认 en，英文）
```

修改后用 `prefix + shift + r`（前缀模式重载配置）即可立即切换，无需重启。

支持的语言代码：`en`（英文，默认）、`zh`（简体中文）。任何以 `zh` 开头的值（如 `zh-CN`、`zh-Hans`）都会识别为中文。

翻译资源在编译时嵌入程序，Linux 的 `herdr` 和 Windows 的
`herdr.exe` 共用同一套 locale 与切换逻辑，不需要在 Windows 包旁边复制
`locales` 目录。Windows 自动构建还会用临时 `HERDR_CONFIG_PATH` 配置实际
执行 `herdr.exe --help`，确认中文资源可被加载后才上传包。

## 二、汉化覆盖范围

| 模块 | 文件 | 大致条数 |
|------|------|---------|
| CLI 命令帮助 | `src/cli/spec.rs` | 123 处 `.about()/.help()` |
| TUI 引导页 | `src/ui/onboarding.rs` | 全部 |
| 模式栏 / 上下文菜单 | `src/ui/menus.rs` | PREFIX/COPY/NAVIGATE/RESIZE 全套 |
| 侧边栏状态词 | `src/ui/sidebar.rs` | done/idle/working/blocked/unknown |
| 导航搜索栏 | `src/ui/navigator.rs` | 切换/搜索/状态/移动/关闭 |
| 对话框 | `src/ui/dialogs.rs` | 重命名/新建工作树/删除/关闭确认 |
| 设置面板 | `src/ui/settings.rs` | 主题/声音/通知/集成等 |
| 状态栏 | `src/ui/status.rs` | 状态词 |
| 快捷键帮助 | `src/ui/keybind_help.rs` | 全部分组与标签 |
| 发布说明 | `src/ui/release_notes.rs` | 标题与页脚 |
| 移动端布局 | `src/ui/mobile.rs` | 全部 |
| 枚举标签 | `src/app/state.rs` | `label()` / `status_label()` |
| 更新流程提示 | `src/update.rs` | 交互提示与错误信息 |
| toast 通知 | `src/app/actions.rs` | needs attention/finished/updated 等 |

**未汉化**（刻意保留）：主题名（catppuccin 等技术标识符）、命令名、VALUE_NAME（WORKSPACE_ID 等）、调试日志（tracing）、panic/assert 消息、release notes 正文（动态数据）。

## 三、架构说明

### 翻译资源
- `locales/en.yml` — 英文原文（fallback）
- `locales/zh.yml` — 简体中文译文

文件**由文件名决定语言**（rust-i18n v3 规范，文件内不要写 `_en:`/`_zh:` 顶层键）。格式：

```yaml
# locales/zh.yml
cli:
  herdr_about: "AI 编程智能体的终端工作区管理器"
status:
  done: "已完成"
  idle: "空闲"
```

**插值语法用 `%{变量名}`**（rust-i18n 3.x，不是 `{变量名}`）：
```yaml
# yml
update:
  checking_channel: "正在检查 %{channel} 通道的更新..."
```
```rust
// 代码
t!("update.checking_channel", channel = channel_name)
```

### 代码改造模式
所有英文字面量改为 `t!("键名")` 调用：

```rust
// 改前
.about("terminal workspace manager for AI coding agents")
// 改后
.about(t!("cli.herdr_about").to_string())

// 改前
Paragraph::new("continue")
// 改后
Paragraph::new(t!("onboarding.continue_btn"))
```

`t!()` 宏在编译期从 `locales/*.yml` 加载翻译，运行时根据全局 locale 选择译文，找不到 key 时 fallback 到 en，再找不到则返回 key 本身。

### 关键文件
| 文件 | 作用 |
|------|------|
| `src/i18n.rs` | `apply_locale()` 封装，调用 `rust_i18n::set_locale()` |
| `src/main.rs` | crate root 调用 `rust_i18n::i18n!("locales", fallback = "en")`；启动时读 config 后调 `apply_locale` |
| `src/app/mod.rs` | 热重载时调 `apply_locale`（prefix+shift+r 生效） |
| `src/config/model.rs` | `UiConfig.language: String`，默认 `"en"` |
| `Cargo.toml` | `rust-i18n = "3"` 依赖 |
| `build.rs` | `cargo:rerun-if-changed=locales/{en,zh}.yml`（修改翻译后自动重编译） |

## 四、如何编译

### 环境要求
- **Rust** ≥ 1.96（stable）
- **Zig** = 0.15.2（精确版本，用于编译 vendored libghostty-vt）

### Windows 编译特别注意（跨盘符 bug）
zig 0.15.2 的 `lib/std/Build/Step/Run.zig:662` 有一个断言：当 Run step 的文件参数与 cwd 在不同盘符时（如 zig 在 `C:`，项目在 `F:`），`std.fs.path.relative` 返回绝对路径，触发 `assert(!std.fs.path.isAbsolute(child_cwd_rel))` panic。

本 Fork 不要求修改 Zig 安装。`build.rs` 在 Windows MSVC 目标下会把 Zig 的全局和本地缓存都放到 `vendor/libghostty-vt/.zig-cache`，确保生成工具、工作目录和文件参数位于同一盘符。自动构建工作流也显式设置同一路径。

如需覆盖默认缓存位置，必须保证两个目录仍与项目处在同一盘符：

```powershell
$cache = Join-Path $PWD "vendor\libghostty-vt\.zig-cache"
$env:ZIG_GLOBAL_CACHE_DIR = $cache
$env:ZIG_LOCAL_CACHE_DIR = $cache
```

这样可以使用官方 Zig 0.15.2，避免对开发机或 CI 工具链做不可追踪的补丁。

### 编译命令
```bash
# 设置 zig 路径（如果 zig 不在 PATH）
export ZIG="/path/to/zig.exe"

cargo check          # 快速语法检查
cargo build --release  # 构建发布版二进制
cargo test --bin herdr  # 运行测试
```

## 五、上游更新维护流程

当 herdr 上游发布新版本时，按以下步骤合并：

```bash
# 必须先把汉化、Windows、UI 等本地修改分别提交，保持工作区干净
python scripts/sync_upstream.py
```

同步工具固定从 `https://github.com/herdrdev/herdr.git` 拉取官方
`master`，先建立备份分支，再以 merge 方式验证候选代码。不要 rebase
`deploy/zh-with-perf`：该分支是可部署的集成历史，改写它会让本地、Fork
远程和自动部署记录相互分叉。遇到冲突或检查失败时，工具会中止合并并
保留同步前的分支与备份，不会自动 stash、push 或部署。

本地 `just check` 只代表当前操作系统通过。共享逻辑必须同时通过 Linux
和 Windows 原生 CI；自动同步也只有在两端针对同一候选提交全部通过后，
才允许推进 `deploy/zh-with-perf` 和部署 Linux。某个平台发现的共享行为
问题应优先补平台无关回归测试，不能只用 `cfg(windows)` 或 `cfg(unix)`
掩盖另一端的问题。

完整的补丁分区和自动化约束见 `.github/FORK_AUTOMATION.md`。

**冲突处理原则**：

1. **`src/` 下的源码冲突**：上游可能新增/修改了英文字符串。优先保留上游的结构改动，然后把新增的英文字符串改为 `t!("新键名")`，并在 `locales/en.yml` 和 `locales/zh.yml` 添加对应条目。

2. **`locales/*.yml` 不会冲突**：这是汉化新增的文件，上游没有，合并无冲突。

3. **`Cargo.toml` / `build.rs` / `src/config/model.rs` / `src/main.rs` / `src/app/mod.rs` / `src/i18n.rs`**：这些是汉化基础设施改动点。如果上游也改了这些文件，需要手动合并——保留 `rust-i18n` 依赖、`language` 字段、`apply_locale` 调用。

**汉化改动点速查**（方便 merge 时定位）：
```
Cargo.toml              — rust-i18n 依赖 + locales include
build.rs                — rerun-if-changed locales
src/main.rs             — i18n!() 宏 + apply_locale + DEFAULT_CONFIG 注释
src/i18n.rs             — apply_locale 封装（新增文件）
src/config/model.rs     — UiConfig.language 字段
src/app/mod.rs          — 热重载 apply_locale
locales/en.yml, zh.yml  — 翻译资源（新增文件）
src/cli/spec.rs         — CLI 帮助 t!() 化
src/ui/*.rs             — TUI 文字 t!() 化
src/app/state.rs        — label() t!() 化
src/update.rs           — 提示/错误 t!() 化
src/app/actions.rs      — toast t!() 化
```

## 六、新增翻译的方法

如果需要翻译一个新的英文字符串：

1. 在源码找到字面量，如 `Paragraph::new("new label")`
2. 选一个键名，如 `dialog.new_label`
3. 改代码：`Paragraph::new(t!("dialog.new_label"))`
4. 在 `locales/en.yml` 加：`new_label: "new label"`（放在对应段下）
5. 在 `locales/zh.yml` 加：`new_label: "新标签"`
6. `python scripts/i18n_key_check.py` 验证源码中的键在中英文词典都存在
7. `cargo check` 验证（build.rs 会检测 yml 变化自动重编译）

`just check`、Windows 原生检查和自动部署门禁都会运行该键检查。缺失键在
rust-i18n 中会回显成 `status.done` 这类点分键名，编译和 Clippy 本身无法
发现，因此不要从任一平台门禁中移除这项检查。

## 七、已知限制

- **动态内容不翻译**：release notes 正文、产品公告正文、toast 里动态拼接的 agent 名等，来自服务端数据或运行时生成，不在静态翻译范围。
- **文档未汉化**：`docs/` 和 `website/` 下的 MDX 用户文档保持英文（独立工程）。
- **主题名等技术标识符**保留英文，避免破坏配置兼容性。
